# RDS (production) — set on EC2/ECS. Local dev defaults to Docker Postgres on localhost.
DATABASE_URL=postgresql+psycopg2://postgres:YOUR_PASSWORD@your-rds-endpoint.region.rds.amazonaws.com:5432/taxi
